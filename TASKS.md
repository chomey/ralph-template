# Tasks

<!--
  Populate this file with discrete, well-defined tasks.
  Each task should be completable in a single Ralph Loop iteration.

  Format:
  - [ ] Task N: Short title — Longer description of what needs to be done [@agent-tag]

  Agent tags: [@frontend], [@backend], [@database], [@devops], [@qa], [@security], [@fullstack]
  Use [ARCH] prefix for architecture/infrastructure tasks.

  Tips for good tasks:
  - Keep them small and atomic (one thing per task)
  - Order them so dependencies come first
  - Be specific about what "done" looks like
  - Include file paths or function names when relevant
  - Tag each task with the best-fit implementation agent
-->

<!-- EXAMPLE TASKS (replace with your own — note UI-first ordering):
- [ ] Task 1: [ARCH] Initialize project & dev server — Scaffold the project so it runs and displays a page in the browser [@devops]
- [ ] Task 2: Set up screenshot & test infrastructure — Install Playwright (or equivalent), create a helper that takes automated screenshots, verify it captures the running app [@qa]
- [ ] Task 3: Build app shell & navigation — Create the main layout, nav bar, and page routing with placeholder pages [@frontend]
- [ ] Task 4: Build core screen with mock data — Implement the primary UI screen using hardcoded/mock data so it's visually verifiable [@frontend]
- [ ] Task 5: [ARCH] Set up data models — Create the core data models per the PRD [@database]
- [ ] Task 6: Wire real data into UI — Replace mock data with real data layer, keeping the UI intact [@fullstack]
- [ ] Task 7: Implement feature X — Build [specific feature] end-to-end (UI + logic) [@fullstack]
- [ ] Task 8: Add auth middleware — Protect API endpoints with JWT validation [@security]
-->
